# 敏感信息（API Key 等）
.env
# .env 的备份同样含密钥（AMAP/ZHIPU/CF），必须一并挡住
.env.bak*

.env.local
.env.*.local
secrets/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/


# ---------------------------------------------------------------------------
# 两条线程：工厂（本地） vs 产品（公开） —— 2026-09-22 定下，别再混回去
# ---------------------------------------------------------------------------
# 本仓库是**产品**：MCP 服务 + 只读数据面 + 对外消费入口。
# 抓取 / 翻译 / 派生重建 / 发布（Pages、R2、push 云端）属于**工厂**，
# 是只在本地跑的后端运维，一律不入库。
#
# MCP 的运行边界（2026-09-22 按 mcp/server.py 的 fetch_text 实参实测）：
#   mcp/**                                 MCP 进程本体（npm 只发这 4 项）
#   data/manifest.json                     分片清单（它的 p 字段就是下面这些路径）
#   data/gb/** · data/en/**                名录分片（zh 284 片 / en 281 片）
#   data/phone-index.jsonl                 号码索引（1 片）
#   skills/registry/fingerprint/**         指纹分片（273 片，搜索召回面）
#   skills/registry/index/**               倒排索引 meta/city/terms（514 片）
#   skills/registry/capability/{id}.json   L1 能力卡（只在 R2，不入库、不在本块内）
#   skills/rfq-kernel/**                   多轮匹配桥接（mcp/server.py 用 sys.path 导入）
#   agent-skill/**                         非 MCP 的对外消费入口（薄框架：SKILL + client_search）
# 除此之外的文件都不参与 MCP 运行 —— 凡不在此清单里的后端脚本，不入库。
#
# ⚠ 本块原先是一份 147 行的 scripts/ 白名单，逐条附「不放行的后果」。
# 那些理由几乎全部成立于同一条旧消费路径：「客户 Agent 照根目录 SKILL.md
# 敲 python scripts/xxx.py」。2026-09-22 起对外消费只剩两条：
#   ① MCP（npm 包 beacon-mfg-mcp，只发 server.py / bin/ / README / RELEASE）
#   ② agent-skill 薄框架（只装 SKILL.md + client_search.py，数据全从 CDN 按需拉）
# 旧理由随之作废，而它的代价是把 fetch / publish / gui / cron 这些后端运维
# 连同它们的依赖一起摆在公开仓库里。
#
# 为什么不能「按目录挑着留」：postfetch.py 用 subprocess 编排了几乎全部脚本，
# 还直接调用 APK/tools/sync_assets.py —— 依赖闭包会把 scripts/、APK/ 整片
# 拖回来。所以边界只能按**「MCP 运行时读什么」**划，不能按
# 「脚本之间互相 import/subprocess 什么」划。后者问的是「工厂能不能跑」，
# 而工厂是本地的、不入库的。
scripts/
APK/
pages/
server/
# ── 零读者残留（2026-09-22 清出）───────────────────────────────────────────────
# 判据：在 mcp/ · skills/rfq-kernel/src/ · agent-skill/ · APK 的 Kotlin 源码里
# grep 不到任何读者（含字符串路径与 import 两种形态）。
# 注：skills/schema/envelope.json 与 rfq-kernel 自己的 schema/envelope.schema.json
# 同名但无关 —— 被读的是后者（kernel.py 的 envelope() 函数与其自带 schema/）。
skills/profiles/
skills/schema/
skills/TEMPLATE.md
skills/README.md
skills/registry/index.json
skills/registry/gb-proc-map.json
skills/registry/category-conflicts.json
data/fetch_cursor.json
data/district_cache.json
# ⚠ skills/vendors/{id}/capability.json **不能**忽略：它是 rfq-kernel
# beacon_adapter._card_path() 在 registry/capability 缺失时的兜底读路径（属 MCP 运行链）。
# ⚠ skills/rfq-kernel/suppliers/** 同理：kernel.load_suppliers() 会 glob 读取。
# ⚠ 脱敏 filter 的 clean 端（scripts/mask_phones.py）随工厂一起不入库，这是**有意**的：
#   · 入库内容本身已是掩码态（138****0000），clone 拿到的就是掩码版；
#   · filter.maskphone.smudge = cat（内建命令），新 clone 的 checkout 不需要任何脚本；
#   · filter.maskphone.clean 的定义只在**本机 .git/config**（.gitattributes 只声明 filter 名），
#     它只对写方（本地工厂）有意义，而工厂天然持有 scripts/ 全量。
#   ⚠ 唯一残留风险：clone 后改 data/** 再 commit，clean filter 缺失 → 会提交未脱敏内容。
#     要兜底就在 CI 里放一个**不依赖 scripts/** 的独立检查（几行 grep），别为此把工厂搬回来。
# CI 只留 MCP 的分发 workflow（两者都只打包 mcp/ 目录），其余 .github 内容不外发。
# ⚠ 注意形态：先 `!.github/workflows/` 放开目录，再逐条 `!` 放行具体文件。
# 只写「放开目录」= 整个 workflows 目录对外 —— validate.yml 就是这样漏出去的。
.github/*
!.github/workflows/
!.github/workflows/mcp-release.yml
!.github/workflows/npm-publish.yml
# release.yml 会把**整个仓库**打包成 zip 发到 GitHub Release（os.walk('.')，
# 只排除 .git/__pycache__/.github）→ 曾是一条「非 MCP 文件对外分发」的通道。2026-09-22 移出。
.github/workflows/release.yml
# validate.yml 跑的是 `python scripts/validate.py --strict` —— 工厂侧脚本，已随工厂移出；
# 留着只会 CI 红灯。它同时是「贡献者提 PR 校验数据」那条旧消费路径的残留。2026-09-22 移出。
.github/workflows/validate.yml
.workbuddy/
examples/
CRON_*.md
.env.example

# ---------------------------------------------------------------------------
# docs/ 发布面收窄（2026-09-22）
# ---------------------------------------------------------------------------
# 根因：**GitHub Pages 的发布根就是 docs/**（实测站点根 200，而 /README.md 404、
# /docs/index.html 404）→ 整目录公开。于是「放进 docs/」等价于「公网可读」。
# 只留落地页 index.html，其余一律算内部材料。
# 不被忽略的后果：反抄袭策略、反爬风控设计（含 App 特征头）、Cloudflare 部署手册、
# token 泄露事故复盘 —— 全部公网可读（2026-09-22 实测 13 个文件均 HTTP 200）。
# 注：下面若干条 docs/xxx 专条规则已被本块覆盖，保留是为了不丢历史原因说明。
docs/*
!docs/index.html

# ---------------------------------------------------------------------------
# 临时文件与本地产物（不入库）
# ---------------------------------------------------------------------------
# _backup/：重写历史前的仓库镜像（git clone --mirror，约 170MB），纯本地保险，
# 恢复方式：git clone --mirror _backup/mirror.git <新目录>。
_backup/
_depgraph.py
_leak_scan.txt
_r2_stale.txt
_r2refresh*.log
_r2verify*.log
_r2_inventory*
_build_precheck.log
# 边界验收探针（2026-09-22）：_mcp_smoke.py 只用「保留下来的文件」跑一次真实检索，
# 用来证明 MCP 运行边界成立。随工厂留在本地，可重复跑；别入库（它 import mcp/server.py
# 需要仓库在工作树里，属本地检查）。
_mcp_smoke.py
_patch_*.py
_gitignore_newblock.txt

# OS
.DS_Store
Thumbs.db

# ---------------------------------------------------------------------------
# 私有层：不进 Git（2026-09-07 清理）
# Git 仓库只放引流层：薄名录 + L0 指纹 + 协议规范。
# 以下都是私有资产，一旦公开等于把商业逻辑送人。
# ---------------------------------------------------------------------------
# 注意：gitignore 不支持行尾注释，`path  # 说明` 会把整行当成一个 pattern，
# 规则静默失效。所有注释必须独占一行。
#
# 私有层 API（auth/api_key、routers/claim、services/ranker）
# ⚠ 这里原来有一条 !server/routers/collect.py 的例外（注册自动发布接线），
# 走的是「放开目录 → server/** 重新忽略全部 → 再放开单文件」三段式。
# 2026-09-22 撤销：server/ 整体归工厂（它是**写方**，客户 Agent 只读 MCP/薄框架，
# 不经过它），而单文件例外会让整目录忽略被穿透 —— 实测 collect.py 正是漏出去的那个。

# 采集脚本
run_fetch.ps1
# 校验脚本
run_validate.ps1
# 运行日志
fetch_log.txt
# 排名算法 + 认证设计（核心商业逻辑）
docs/API_RANKING_AUTH_DESIGN.md
# 内部修复记录
docs/fix_doc_number_drift_*.md

# ---------------------------------------------------------------------------
# 商业策略文档与原型（2026-09-08）
# 写明了护城河分析、定价策略、采集流程，公开等于把打法送人
# ---------------------------------------------------------------------------
docs/PROPOSAL_V2_COMMERCIAL.md
docs/PROPOSAL_VENDOR_SKILL.md
docs/PROPOSAL_A2A_OUTBOUND.md
docs/prototype/

# ---------------------------------------------------------------------------
# Skill 分层存储（2026-09-07 起）
#
# L0 指纹  skills/registry/fingerprint/*.jsonl  → 进 Git（客户 Agent 全量扫这层做初筛）
# L1 能力卡 skills/vendors/*/capability.json    → 不进 Git（走云端按需拉取）
# L2 自述   skills/vendors/*/SKILL.md           → 不进 Git（走云端按需拉取）
#
# 理由：1200+ 家 × (SKILL.md + capability.json) ≈ 11MB 且持续增长。
# 客户 Agent 只需 L0 指纹即可完成 95% 的初筛淘汰，命中后才调 API 取 L1/L2。
# 详见 docs/AUTO_PROFILE_REPORT.md
# ---------------------------------------------------------------------------
skills/vendors/*/SKILL.md
skills/vendors/*/capability.json
skills/vendors/*/assets/
skills/registry/capability/

# 重跑批量整理时归档的旧能力卡（reset_auto.py 生成，确认无误后可自行删除）
skills/_vendors_backup_*/
skills/_keep_sample/

# 例外：保留 1 家已认主样板进 Git，作为「标准长什么样」的参考
!skills/vendors/CN-MFG-0000005/SKILL.md
!skills/vendors/CN-MFG-0000005/capability.json

# 运行日志与临时文件
*.log
# 单实例抓取锁（fetch_batch.py 仓库内 PID 锁，防双实例并发抓取到账本）
.fetch_batch.lock
_fixed_*_backup.json
en_backfill_*
en_watchdog*
cjk_fix_*
# 列式导出产物（构建物，不进 Git）
dist/
# 部署打包的中间产物（deploy_pages.py 生成，200KB 压缩包，不进 Git）
pages/dist.zip
# wrangler 的本地状态目录（部署记录、binding 同步缓存），机器相关，不进 Git
.wrangler/

# ---------------------------------------------------------------------------
# 安卓客户端（APK/）
# 构建产物与本地配置不进 Git；内置 assets 由 tools/sync_assets.py 从仓库数据
# 重新生成，避免 4.76MB 指纹在仓库里存两份（原始来源 skills/registry/fingerprint 已入库）
# ---------------------------------------------------------------------------
APK/.gradle/
APK/build/
APK/app/build/
APK/local.properties
APK/app/src/main/assets/
# 构建产物：19MB 的 apk 不进仓库（要装机的包走本地构建，见 APK/build.sh）
APK/*.apk
APK/app/src/androidTest/assets/e2e_expect.json

# ---------------------------------------------------------------------------
# Excel 收集模板（生成物，不入库）
# 企业信息收集模板.xlsx 由 scripts/gen_excel_template.py 动态读取
# skills/profiles/*.json 生成，schema 变了重跑即可；入库会和 schema 产生
# 两份真相，且容易被 git add -A 卷进去。生成器脚本本身入库。
# ---------------------------------------------------------------------------
企业信息收集模板.xlsx

# ---------------------------------------------------------------------------
# 项目调试说明.md：本地调试手册，不入库（2026-09-18，user 决定）。
# 它记录的是本机环境坑、避坑清单与内部排障过程，属于本地工作笔记；
# 它曾在 859fad5 误入版本库，已用 `git rm --cached` 取消跟踪（本地文件保留）。
# ---------------------------------------------------------------------------
项目调试说明.md
# ---------------------------------------------------------------------------
# 去重清洗前的 data/gb 快照（scripts/dedup_gb.py --apply 自动生成）。
# 体积大（~170MB）且只在出事故回滚时用得到，不入库。2026-09-19
# ---------------------------------------------------------------------------
.backup_dedup_*/

# ---------------------------------------------------------------------------
# 后端/运维内容：非 MCP 检索所需，不进 Git（2026-09-19 用户明确口径）
#
# Git 仓库只放「MCP 工作必需」的内容：发布数据（data/、fingerprint、index）+
# L2 自述（skills/vendors/*/SKILL.md）清单 + 协议规范 + MCP server（mcp/）。
# 采集/翻译/维护流水线、本机运维工具、审计日志等后端产物一律不入库。
# Cloudflare 发布面由 deploy_pages.build() 控制，本就只含 data/ + fingerprint/
# + index/ + vendors SKILL.md + 能力卡分片，不含任何脚本/运维文件。
# 与既有口径一致：项目调试说明.md / CRON_*.md / docs 内部材料同规则。
# （scripts/ 下的运维留档已由上方 `scripts/*` 整体忽略，无需重复列举。）
# ---------------------------------------------------------------------------
# 构建产物：Android 安装包（含仓库根目录下误入的包；上方 APK/*.apk 只覆盖子目录）
*.apk
# 数据质量检查报告（后端 QA 产物，非发布数据）
QA_*.md
# 去重审计日志（scripts/dedup_gb.py 按需再生成，非发布数据）
data/_dedup_removed_ids.json
# 本机 cron 管理 UI（运维工具，非 MCP 检索所需；与 项目调试说明.md 同口径）
cron_manager.py
cron_manager.html
start_cron_manager.bat
# 遗留备份目录（旧 ID 制式能力卡备份；postfetch 的提交步骤本就明确不碰它）
.backup_legacy/
