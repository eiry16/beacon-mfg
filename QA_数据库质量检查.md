# 数据库质量检查报告

> 生成时间：2026-09-19 · 数据基线：127,738 条中文记录（`data/gb`，277 个分片）
> 只读取证，未改动任何数据。

---

## 结论速览

| # | 检查项 | 结论 | 量化 |
|---|---|---|---|
| 1 | 目标城市 × 行业是否全覆盖 | **否，但缺口可控** | 城市×行业组合 735 个，零结果 **46 个（6.3%）** |
| 2 | 是否存在同一企业重复编号 | **是** | 确凿重复 **856 条（0.67%）**；放宽口径 **1,299 条（1.02%）** |
| 3 | 抓取脚本是否有去重保护 | **有，但键设计有缺陷** | 4 个缺陷，见下文 |

---

## 一、覆盖面：城市 × 行业

### 1.1 任务级（行业码 × 关键词 × 城市）

| 指标 | 数值 |
|---|---|
| 期望任务（JOBS 矩阵展开） | 2,515 |
| 游标已登记 | 1,932 |
| **从未抓取** | **827（32.9%）** |
| 矩阵外历史遗留任务 | 244 |

按城市看缺口比例：温州 81%、中山 68%、绍兴/惠州/珠海 67%、宁波 52%、东莞 52%……
西南四城（贵阳/南宁/昆明/遵义）反而是 **0% 缺口**（新铺，优先跑完了）。

> 说明：日配额 1000 次请求 ≈ 每天 125 个任务，2,515 个任务轮一遍要 ~20 天。
> 所以任务级缺口主要是**调度排队**，不是设计缺陷。

### 1.2 落库级（城市 × 行业码）—— 这是真正要关心的

| 指标 | 数值 |
|---|---|
| 期望组合 | 735 |
| **零结果组合** | **46（6.3%）** |

拆分这 46 个：

| 成因 | 数量 | 明细 |
|---|---|---|
| 从未抓取 | 40 | 涉及 17 个行业，集中在 extended 层 |
| 抓过但 0 落地 | 6 | 昆山×4（3484/3311/2929/3525）、昆明 3989、遵义 3251 |

**从未抓取的行业 Top：** 3393 锻件及粉末冶金(5)、3481 金属密封件(4)、3072 卫生陶瓷(4)、
3983 传感器(4)、2921 塑料薄膜(3)、6130 民宿(3)……

**全库零结果且从未抓取的行业（3 个）：**

| 代码 | 行业 |
|---|---|
| 3072 | 卫生陶瓷制品制造 |
| 3481 | 金属密封件制造 |
| 3983 | 敏感元件及传感器制造 |

### 1.3 ⚠️ 昆山不是真空洞 —— 是城市标签错位

期望但库中 0 记录的城市有 5 个：昆山、海盐、莫干山、大理、丽江。

但实测地址含这些地名的记录**是存在的**，只是 `region.city` 写了上级地级市：

| 地址含 | 实际 city 字段 | 条数 |
|---|---|---|
| 昆山 | **苏州** | 190 |
| 昆山 | 上海 | 22 |
| 海盐 | **嘉兴** | 22 |
| 莫干山 | **杭州** | 33 |
| 大理 / 丽江 | 贵阳/嘉兴/遵义… | 各 ≤6 |

**根因：** 高德 POI 对县级市返回地级市名，`normalize_region()` 直接用该值。
**后果：** 昆山的 4 个行业游标显示已抓取且 `last_seen` 很高（如 `2929|塑料模具|昆山` = 200 条、
`3484|精密机械|昆山` = 200 条），数据确实抓到了，但全落在苏州名下 →
**按「昆山」检索返回 0**，对 MCP / App 是真实的可用性问题。

大理、丽江则是 3/3 任务从未执行，属真缺口。

---

## 二、重复：同一家企业被分配不同编号

### 2.1 确凿重复（同一 `amap.poi_id` 有多个 `id`）—— 856 条冗余

| 指标 | 数值 |
|---|---|
| 唯一 poi_id | 120,877 |
| **重复 poi_id 组** | **851** |
| 涉及记录 | 1,707 → **冗余 856 条（0.67%）** |

**成因定性（关键证据）：**

| 特征 | 占比 | 说明 |
|---|---|---|
| ID 间距 ≤ 50（同一批次） | **844 / 851** | 同一次 `save_suppliers()` 内产生 |
| 同城市 | 851 / 851 | 不是跨城市误判 |
| 同搜索关键词 | 849 / 851 | 不是多关键词重复召回 |

典型案例（ID 连续、名称/地址/poi_id/关键词四者全同）：

```
poi_id=B0L0USVJZ3
  CN-MFG-0090495 | 肆花社花园餐厅 | 贵阳 | 黔灵镇三桥村二桥路16号附3号 | kw=餐厅
  CN-MFG-0090502 | 肆花社花园餐厅 | 贵阳 | 黔灵镇三桥村二桥路16号附3号 | kw=餐厅
poi_id=B0JKBD96EC
  CN-MFG-0093087 | 贵阳正新街网红厕所馒头 | 贵阳 | 中华中路8号时代广场… | kw=便当
  CN-MFG-0093088 | 贵阳正新街网红厕所馒头 | 贵阳 | 中华中路8号时代广场… | kw=便当
```

### 2.2 放宽口径：归一化名称 + 归一化地址完全相同 —— 1,299 条冗余

| 指标 | 数值 |
|---|---|
| 重复组 | 1,265 |
| 涉及记录 | 2,564 → **冗余 1,299 条（1.02%）** |
| 组内 poi_id 相同 | 889 组（高德同一 POI） |
| 组内 poi_id 不同 | **376 组（高德对同一实体建了多个 POI）** |

poi_id 不同但明显同一家的例子（老数据无 poi_id，新抓取补了 poi_id → 一对二）：

```
云创广告图文装饰 / 迈皋桥街道怡宁路183号
  CN-MFG-0007025  poi=None        kw=None
  CN-MFG-0008736  poi=B0MGY76XZX  kw=亚克力加工
创象金属激光切割剪板折弯刨槽焊接 / 长安镇红色村王家坝46号
  CN-MFG-0014488  poi=None        kw=None
  CN-MFG-0012879  poi=B0JGZRGTEP  kw=激光切割  code=3311
```

### 2.3 不能算重复的：连锁分店

| 指标 | 数值 |
|---|---|
| 同名但地址不同 | 4,873 组 / 28,969 条 |

绝大多数是合法的不同门店，**不应合并**：

| 城市 | 名称 | 条数 |
|---|---|---|
| 上海 | 星巴克 | 611 |
| 上海 | 瑞幸咖啡 | 532 |
| 武汉 | 瑞幸咖啡 | 486 |
| 苏州 | 瑞幸咖啡 | 271 |
| 深圳 | 郑远元专业修脚房 | 141 |

> 注意：这些名字带分店后缀（如「星巴克(XX店)」），原始名各不相同，
> 所以现有精确名去重**拦不住它们**（这是对的），只有归一化后才聚到一起。

### 2.4 电话重复（仅供参考，不宜作去重依据）

2,534 组多记录电话 / 8,742 条 —— 多为连锁总机、代收点、快递柜，误伤率高。

---

## 三、现有去重机制：有，但有 4 个缺陷

**位置：** `scripts/fetch_gaode_poi.py:329 save_suppliers()`
**索引：** `scripts/gb_store.py:615 scan_cache()` → `names: dict[公司名] = (bucket, id)`
**语义：** 命中已存在则**不新增**，只做「累加关键词 / 补全 amap 字段 / 升级电话」。
**范围：** 全库（跨国标小类），这一点是对的。

### 缺陷 1（主因）：批内不去重 —— 分配 ID 前 `names` 未更新

```python
for p in pois:                    # 第一遍：只查
    hit = names.get(name)
    if hit: ...; continue
    new_pois.append(p)            # ← 这里没写 names！

for i, p in enumerate(new_pois):  # 第二遍：才写
    rec = to_supplier(p, ..., seq_start + i, ...)
    names[p["name"]] = (bucket, rec["id"])   # ← 太晚了
```

同一批 `pois` 里出现两个同名，第一遍都判为「新企业」，各拿一个编号。
而 `fetch()` 的翻页是 `pois_all.extend(pois)`，**没有任何批内去重**，
高德翻页会重复召回同一 POI → 直接命中这个缺陷。

**这就是 856 条确凿重复的主要来源（844/851 组 ID 间距 ≤50）。**

### 缺陷 2：键不含 `poi_id`，高德主键没用上

记录里明明存了 `amap.poi_id`，但去重却用公司名。
同一 POI 换个别名/分店后缀就漏网（缺陷 1 之外的 376 组属此类）。

### 缺陷 3：键不含城市 —— 跨城市同名不同实体会被误吞

`names` 是**全局精确公司名**，不带城市。第 2 个城市的同名实体会被判为「已存在」而丢弃，
且只做关键词累加，**不会新建记录** → 静默丢数据。

当前观测到 10 个名字跨 2 城（如 固特异轮胎: 宁波/常州、激光切割钣金加工: 上海/苏州）。
实际损失量无法直接量化（被吞掉的不会留下痕迹）。

### 缺陷 4：精确名匹配，无归一化

「XX公司」与「XX公司(分店)」判为两家；反之若名称字符串恰好一致但实体不同则误合。

---

## 四、建议的修复（按优先级）

| 优先级 | 动作 | 预期效果 |
|---|---|---|
| P0 | `fetch()` 返回前按 `poi_id` 收敛 `pois_all` | 堵住翻页重复召回 |
| P0 | `save_suppliers()` 第一遍循环内**即时**写 `names` | 堵住批内漏判 |
| P1 | 去重键升级：**有 `poi_id` 用 `poi_id`**，否则用 `(城市, 归一化名称+地址)` | 消除缺陷 2/3/4 |
| P1 | 存量清洗：按 poi_id 合并 856 条 + 按 (城市,归一化名+地址) 合并 1,299 条，keywords 并入保留记录 | 消除历史冗余 |
| P2 | 补跑 3 个零任务行业（3072/3481/3983）+ 大理/丽江 | 补齐覆盖 |
| P2 | 县级市归属：昆山→苏州 等建立别名映射，检索时双向命中 | 解决「查昆山返回 0」 |

---

*分析方法：读取 `data/gb/**/*.json` 全量记录，按 `amap.poi_id` / 归一化名称+地址 /
电话 / 名称+坐标 四个维度交叉验证；覆盖面对照 `scripts/fetch_batch.py` 的 `JOBS`
矩阵与 `data/fetch_cursor.json` 游标。*

---

# 修复执行结果（2026-09-19 同日，「P0 + P1 + P2 全部执行」）

## 一、P0 批内去重 —— 已修

| 文件 | 改动 |
|---|---|
| `fetch_gaode_poi.py` `fetch()` | 翻页返回前按 `poi_id` 收敛 `pois_all`（原来 `extend` 后完全不去重） |
| `fetch_gaode_poi.py` `save_suppliers()` | **两遍循环合成一遍**：遍历即判重、即发号、即登记索引 |

> ⚠️ 中途踩坑记录：第一版只把 `register_target()` 挪进第二遍循环，执行顺序仍是
> `find → find → register → register`，批内去重依旧失效 —— 回归测试立刻抓出来了。
> 必须让「登记」与「发号」在同一个循环体内发生才有效。

**实战验证**：补跑日志出现 `翻页去重：去掉 4 条重复召回（200 → 196）`。
新增回归测试 `scripts/test_dedup.py`，13 个用例全绿。

## 二、P1a 判重键升级 —— 已修

`gb_store.scan_cache()` 由「全局精确公司名」单键，改为三级：

| 优先级 | 键 | 解决什么 |
|---|---|---|
| 1 | `amap.poi_id` | 高德主键，同一 POI 必同一实体 |
| 2 | `(城市, 归一化名, 归一化地址)` | poi_id 缺失 / 高德重复建号 |
| 3 | `(城市, 公司原名)` | 旧口径，收窄到同城 |

新增 `norm_name()` / `norm_addr()` / `find_duplicate()` / `register_target()`。
`norm_name` 会剥括号（分店名）、标点、`有限公司/厂/店` 后缀 ——
**但绝不单独作为判重键**（会把星巴克 611 家门店并成一条），必须与地址叠加。

> 兼容性提示：`names` 的键结构从 `str` 改成 `(city, name)` tuple。
> 老代码若按字符串取值会全部 miss（等价于不去重）。全仓只有 `save_suppliers` 用它。

## 三、P1b 存量清洗 —— 已执行

新脚本 `scripts/dedup_gb.py`（`--dry-run` 默认，备份后 `--apply`）。

| 项 | 清洗前 | 清洗后 |
|---|---|---|
| 磁盘条目 | 127,738 | **126,434** |
| 唯一 id | 127,569（含幽灵） | **126,434** |
| 跨文件重复 id（幽灵数据） | 169 | **0** |
| 同 poi_id 多编号 | 851 组 | **0** |
| 真重复冗余 | 1,135 条 | **0** |

合并语义：保留信息最全的一条（有电话 > 有经纬 > 有 type > 有 poi_id > 编号最小），
关键词取并集，缺失字段只补不覆盖。

## 四、英文镜像重复（新发现，已修）

| 项 | 数值 |
|---|---|
| en 跨分片重复 id | 15,989（占英文库 12%） |
| 累计清理冗余 | ~18,000 条 |

**根因**：`en_backfill.flush()` 整桶写主文件并删 `-pN`，而 `gb_store` /
`en_sync_industry` 都按 `MAX_PER_FILE=5000` 分片 —— 两套布局并存时，
主文件与 `-p2` 各持一批相同 id。

**修复**：`en_backfill` 新增 `_write_en_sharded()` / `_atomic_write_json()`，
统一按 `gb_store.MAX_PER_FILE` 分片；存量用 `dedup_gb.py --dedupe-en` 清掉。

> 注意：守护进程若仍持有旧代码会边写边复发，必须让它重启加载新代码。

## 五、P2 覆盖补齐 —— 已执行

| 动作 | 结果 |
|---|---|
| 补跑 3072 / 3481 / 3983（20 任务） | **+1,163 条** |
| 补跑 大理 / 丽江（6 任务） | **+1,092 条** |
| 合计 | **+2,255 条** |

### 县级市归属（`region.district` 新字段）

昆山 / 海盐 / 莫干山 在高德里 `cityname` 返回的是**地级市**（昆山 → 苏州），
所以数据抓得到、但按「昆山」检索永远 0 条。

- 新增 `region.district`（schema 已同步）；
- 新脚本 `scripts/backfill_district.py`：用 `district_cache.json` 的 adcode→区县
  加地址兜底，回填 **78,165 条 / 246 个桶**；
- `to_supplier()` 抓取时从 `adname` 写入；
- 下游打通：`gen_fingerprint` 加 `dist`、`gen_search_index` 的 city.json 并入区县键、
  `mcp/server.py` 三处 city 过滤改为 `city in (rec.city, rec.dist)`、`_hay` 加 `dist`。

实测：`district=昆山` **2,094 条**、`district=海盐` **187 条**。
莫干山仍为 0 —— `district_cache.json` 里它的值是 `"none"`（查不到行政区划），需另行处理。

## 六、修复后全库状态

| 指标 | 值 |
|---|---|
| 中文记录 | 128,689 |
| 英文镜像 | 111,967（翻译守护仍在补齐剩余约 1.7 万条） |
| 国标覆盖率 | 98.3%（126,493 / 128,689） |
| `validate.py --strict` | **通过**（2 个 WARN：3484/3989 有 1340 条电话仍为「待核实」） |
